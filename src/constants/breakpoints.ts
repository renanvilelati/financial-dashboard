const sizes = {
  sm: '640px',
  md: '1024px',
  lg: '1400px',
}

export const breakpoints = {
  sm: `(max-width: ${sizes.sm})`, 
  md: `(max-width: ${sizes.md})`, 
  lg: `(max-width: ${sizes.lg})`, 
}

